const fs = require('fs'); const content = fs.readFileSync('src/services/productService.js', 'utf8'); const regex = /id: '([^']+)',\s*name: ['"]([^'"]+)['"]/g; let match; let products = []; while ((match = regex.exec(content)) !== null) { products.push({ id: match[1], name: match[2] }); } fs.writeFileSync('products.json', JSON.stringify(products, null, 2)); console.log('Total products:', products.length);
